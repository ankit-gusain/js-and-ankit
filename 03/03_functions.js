//object and functions
let student1 = {
    username: "sam",
    gender: "male",
    age: 34
}

function display(anyobj) {
    return `student with name ${anyobj.username} with gender ${anyobj.gender} has age ${anyobj.age}`;

}

console.log(display(student1));

//arrays and function
let student2 = [
    sname = "gamer",
    gender = "male"

]

function disp(getArray) {
    return `student ${student2[0]} has gender ${student2[1]}`;
}

console.log(disp(student2));